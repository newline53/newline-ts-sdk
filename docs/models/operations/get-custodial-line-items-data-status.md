# GetCustodialLineItemsDataStatus

Other statuses such as `pending` will be available later

## Example Usage

```typescript
import { GetCustodialLineItemsDataStatus } from "newline-ts-sdk/models/operations";

let value: GetCustodialLineItemsDataStatus = "voided";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"settled" | "voided" | Unrecognized<string>
```