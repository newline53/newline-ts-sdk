/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { NewlineSDKCore } from "../core.js";
import { encodeFormQuery } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import { NewlineError } from "../models/errors/newline-error.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List Transactions
 *
 * @remarks
 * Retrieves a list of Transactions. Transactions representing expired authorizations or expired reversals are suppressed by default.
 */
export function transactionsList(
  client: NewlineSDKCore,
  request?: operations.GetTransactionsRequest | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.GetTransactionsResponse,
    | NewlineError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    request,
    options,
  ));
}

async function $do(
  client: NewlineSDKCore,
  request?: operations.GetTransactionsRequest | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.GetTransactionsResponse,
      | NewlineError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(
        z.optional(operations.GetTransactionsRequest$outboundSchema),
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/transactions")();

  const query = encodeFormQuery({
    "created_at_after": payload?.created_at_after,
    "created_at_before": payload?.created_at_before,
    "customer_uid": payload?.customer_uid,
    "destination_synthetic_account_uid": payload
      ?.destination_synthetic_account_uid,
    "has_return": payload?.has_return,
    "id_after": payload?.id_after,
    "id_before": payload?.id_before,
    "include_zero": payload?.include_zero,
    "initial_action_at_after": payload?.initial_action_at_after,
    "initial_action_at_before": payload?.initial_action_at_before,
    "limit": payload?.limit,
    "offset": payload?.offset,
    "pool_uid": payload?.pool_uid,
    "search_description": payload?.search_description,
    "settled_at_after": payload?.settled_at_after,
    "settled_at_before": payload?.settled_at_before,
    "settled_index_after": payload?.settled_index_after,
    "settled_index_before": payload?.settled_index_before,
    "show_denied_auths": payload?.show_denied_auths,
    "show_expired": payload?.show_expired,
    "sort": payload?.sort,
    "source_synthetic_account_uid": payload?.source_synthetic_account_uid,
    "status": payload?.status,
    "synthetic_account_uid": payload?.synthetic_account_uid,
    "type": payload?.type,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "get_/transactions",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.GetTransactionsResponse,
    | NewlineError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, operations.GetTransactionsResponse$inboundSchema, {
      hdrs: true,
      key: "Result",
    }),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
