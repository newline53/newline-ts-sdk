# GetCustodialLineItemsUidStatus

Other statuses such as `pending` will be available later

## Example Usage

```typescript
import { GetCustodialLineItemsUidStatus } from "newline-ts-sdk/models/operations";

let value: GetCustodialLineItemsUidStatus = "voided";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"settled" | "voided" | Unrecognized<string>
```